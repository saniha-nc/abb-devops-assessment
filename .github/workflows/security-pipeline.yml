name: Security Scan Pipeline

on:
  push:
    branches: [ "feature/security" ]

jobs:
  security-scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run security scan
      run: |
        echo "Scanning dependencies..."
        echo "Vulnerability found in library"
        echo "Fix applied"

